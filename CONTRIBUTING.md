# 開発の手引き

このプラグインに手を入れたい人向けです。**YMM4 を持っていなくても、ビルドとテストはできます。**

## まず動かす

```powershell
pwsh Ymm4Stubs/build-stubs.ps1          # YMM4 の型だけを真似た空の DLL を作る
$env:YMM4_DIR = "$PWD/Ymm4Stubs/dist"
dotnet test KakiniwaYmm4Import.Tests    # 純ロジックのテスト
```

実際に YMM4 で動かすには本物が要ります(`$env:YMM4_DIR` をインストール先に戻して
`pwsh build-plugin.ps1`)。

## このリポジトリはミラーです

開発は書き庭本体のモノレポで行い、リリースのたびにここへ一方向同期しています。
**直接コミットしたものは次の同期で消えます。** PR は歓迎ですが、取り込みは
「ここでマージ」ではなく上流へ反映してから同期し直す形になります。詳しくは
[README](README.md) の「このリポジトリについて」を見てください。

## コードの地図

| ファイル | 役割 | 行数の目安 |
|---|---|---|
| `KakiniwaYmm4Import/Plugin.cs` | YMM4 のツールメニューに載る入口。設定の読み書き | 小 |
| `KakiniwaYmm4Import/ImportUi.cs` | 取り込みダイアログ(話者割り当て・配置先・ログ表示) | 中 |
| `KakiniwaYmm4Import/PackModel.cs` | 受け渡しパック(timeline.json)の型・読み込み・検証 | 中 |
| `KakiniwaYmm4Import/Importer.cs` | **配置の本体**。ここが一番大きい | 大 |
| `KakiniwaYmm4Import/Ymm4Interop.cs` | YMM4 本体をリフレクションで扱う層 | 大 |
| `KakiniwaYmm4Import/MediaProbe.cs` | 音声の実尺・画像寸法をヘッダ直読み | 小 |
| `KakiniwaYmm4Import/LayerPath.cs` | PSD レイヤーパスの相互変換 | 小 |

### Importer.Run() の段取り

`Run()` は長い1つのメソッドです(約960行)。読むときは、この順番で進むと思ってください。

1. **下ごしらえ** — アクティブなタイムラインと MainModel を探す。
   **取り込み前から在ったアイテムを控える**(後段の再配置・切り詰めは「今回置いた分」だけを
   対象にする。これが無いと既存の作業まで壊す)
2. **配置先を決める** — 開始レイヤーと開始フレーム(先頭 or カーソル位置)
3. **レーン割り当て(2パス)** — 実際に使う種類だけを 0 から詰める。固定表ではない。
   重なり順は 背景 < BGM < SE < ト書き < キャラ < 小物 < エフェクト < 窓 < ボイス。
   小物は「時間が重ならないものを同じレーンに畳む」(区間グラフの貪欲彩色 =
   `AssignGreedySlots`)
4. **イベントを順に置く** — セリフ・立ち絵・表情・背景・BGM・SE・小物・窓・エフェクト。
   立ち絵の切替や退場では、前のアイテムの尺をその時点で切り詰める
5. **ボイスの実尺を待つ** — YMM4 が音声を生成し終えるまで待つ(`WaitForVoiceLengths`)
6. **実尺で並べ直す** — 見積り時間で置いたものを、実際の音声の長さで前から詰め直す
   (`ResequenceByVoiceLength`)。立ち絵・表情の終端もボイスに合わせる
7. **後始末** — 衝突解決・診断ログ・声の設定メモの案内

### 手を入れやすいところ

`Run()` の外にある `internal static` の関数は、**YMM4 が無くても単体で試験できます**。
判断だけを切り出してあるので、ここを直すのが一番安全です。

- `AssignGreedySlots` — 小物のレーン畳み
- `DecideTachieHostName` / `PlanPortraitSwitch` / `PortraitCutTachieNewLength` — 立ち絵の切替
- `FindPortrait` / `PortraitAssignedName` / `FindYmm4CharacterByName` — 名前の解決
- `ShiftDeltaAt` — 再配置のずらし量

新しい判断を足すときも、**まず純関数として切り出してテストを書く**のがこのコードベースの
流儀です。`Run()` の中に条件を書き足すと、誰も試験できなくなります。

## 気をつけること

### YMM4 の非公開 API に依存しています

`MainModel.AddVoiceItemAsync`・`Timeline.AddItems` などを**リフレクション経由**で
呼んでいます(公式に公開されている API ではありません)。YMM4 の更新で動かなくなる
可能性があり、その場合は**ログに理由を出して安全側=何もしない**に倒す設計です。

新しく本体の機能を触るときは、`KakiniwaYmm4Import/Ymm4Interop.cs` の既存のやり方に合わせてください。
名前だけでなく**引数の形まで確認してから呼ぶ**(オーバーロードが増えた版で別物を
呼ばないため)というのが、そこでの決まりです。

### 利用者のプロジェクトを書き換えます

取り込みは元に戻しにくい操作です。途中で失敗したら、**半端に配置された状態が残ります**。
失敗はログに流すだけにせず、利用者に見える形で伝えてください(いまはダイアログを出し、
Ctrl+Z か保存せずに開き直すよう案内しています)。

### 受け渡し形式は書き庭が決めています

timeline.json の仕様は [docs/pack-format.md](/docs/pack-format.md) が正で、
**書き庭が定める規格**です。このリポジトリにあるのは配布用の写しなので、ここを直しても
上流には届きません。仕様への提案は Issue へどうぞ。

`fixtures/timeline.golden.json` は書き庭側と共有している契約ファイルです。これを変えると、
上流では書き出す側のテストも落ちます(片側だけ変えられないようにしてあります)。

## テストの書き方

- **ソースを読んで文字列を探すテストは書かない**。条件を反転しても通ってしまい、
  リファクタで壊れます。実際に関数を動かして確かめてください
- **期待値は実装と独立に用意する**。実装と同じ式で期待値を作ると、何も検査していません
- 新しいガードを入れたら、**それを一時的に外すとテストが赤くなること**を確かめてください

## 不具合を報告するとき

再現に必要なので、**YMM4 のバージョン**と**取り込みダイアログのログ**を添えてください。
このプラグインは YMM4 の版によって動きが変わりえます(検証済みの版は README にあります)。
