<!--
  ありがとうございます。送る前に1つだけ知っておいてください。

  ★このリポジトリは書き庭本体からの**一方向ミラー**です。いただいた変更は、この
    リポジトリでマージするのではなく、**上流(書き庭のモノレポ)へ反映してから同期し直す**
    形で取り込みます。そのため PR は「マージ」ではなく「クローズ」されますが、
    変更が捨てられたわけではありません(次のリリースで反映されます)。

    直接ここへコミットしたものは、次の同期で消えてしまう仕組みです。
    詳しくは CONTRIBUTING.md を見てください。
-->

## 何を変えましたか

<!-- 直したこと・足したことを、ひとことで -->

## なぜ

<!-- どういう場面で困ったか。不具合なら、再現の手順があると助かります -->

## 確かめたこと

<!-- 例: dotnet test が通る / YMM4 v4.55.1 で実際に取り込んでみた -->

- [ ] `dotnet test KakiniwaYmm4Import.Tests` が通る
      (YMM4 が無くても動かせます。CONTRIBUTING.md の「まず動かす」を参照)
- [ ] 実際の YMM4 で動かして確かめた(できれば。版も書いてください: )

## 気にしていること

<!--
  自信が無いところ、判断を委ねたいところがあれば書いてください。
  特にこのあたりは相談してもらえると助かります:
    - YMM4 の非公開 API を新しく触る変更(版によって壊れるため)
    - 受け渡し形式(timeline.json)に関わる変更。仕様の正は書き庭側にあります
    - fixtures/timeline.golden.json を変える変更。書き庭側のテストも落ちます
-->
