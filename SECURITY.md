# セキュリティについて

## 報告のしかた

見つけたものが**他の人の環境で悪用されうる**なら、公開の Issue ではなく
[GitHub のセキュリティ報告](../../security/advisories/new)から知らせてください
(非公開でやり取りできます)。それ以外の不具合は、ふつうに Issue で構いません。

判断に迷ったら、**とりあえず非公開で送ってください**。公開してよいものだったら、
こちらから Issue に移します。

## このプラグインで気にしている範囲

ローカルで動くプラグインなので、ネットワーク越しの攻撃はありません。実際に気にして
いるのは「**受け渡しパックの中身は必ずしも信用できない**」という点です。パックは
書き庭が書き出したものとは限らず、他人からもらったものを取り込むこともありえます。

そのため、次のような入力は**弾くか、無害な扱いに倒す**設計にしています。

- **素材のパスで作品フォルダの外を指す**もの(`..` を含む、絶対パス、UNC パス)。
  パック由来の相対パスは必ず「パックのフォルダ配下か」を確かめてから使い、
  **確認を通す前に `File.Exists` を呼びません**。
  - 絶対パス(`C:\Users\…\.ssh\id_rsa` など)や `..\` を許すと、細工したパックで
    任意のローカルファイルを参照・読取できてしまう
  - UNC(`\\attacker\share\a.png`)は、**存在を確かめるだけで**取り込んだ人の
    NTLM ハッシュが攻撃者の SMB サーバへ送られます。だから存在確認そのものを、
    配下かどうかの判定より後ろに置いています
- **PSD レイヤーパスや名前に紛れ込ませた区切り文字**。ファイルの場所を決める値に
  そのまま使わない
- **見積り時間や寸法の異常値**(負・巨大)。そのまま計算に使うと配置が壊れる

ここを通り抜けて「パックを取り込んだだけで、関係ないファイルを読まれた・書かれた」が
起きるなら、それは報告してほしい種類の不具合です。

## 気にしていない(仕様として受け入れている)こと

- **YMM4 の非公開 API をリフレクションで呼んでいること**。公式に公開された API では
  ないので、YMM4 の更新で動かなくなりえます。その場合はログに理由を出して
  「何もしない」に倒します(壊れた状態で書き込まない)
- **取り込みは元に戻しにくい操作であること**。プロジェクトを保存してから実行してください。
  途中で失敗したときは、その旨をダイアログで知らせます

## 対象のバージョン

いま公開している版だけを見ています。過去の版に遡って直すことはしません。
検証済みの YMM4 の版は [README](README.md) にあります。
